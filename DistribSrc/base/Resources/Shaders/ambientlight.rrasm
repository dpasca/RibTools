;	light
;	ambientlight(
;		float intensity = 1;
;		color lightcolor = 1;)
;	{
;		Cl = intensity * lightcolor;
;	}

.data
	Cl			global varying color

	intensity	parameter uniform float 1
	lightcolor	parameter uniform color 1


.code

light
	mul.vvs		Cl	lightcolor	intensity

	ret
