
* Compiler

  sudo apt-get install g++		# for GCC/g++ !!


* IDE
  
  sudo apt-get install codeblocks	# for Code::Blocks

* Dependencies

  sudo apt-get install mesa-common-dev	# for gl.h
  sudo apt-get install libglu1-mesa-dev	# for glu.h
  sudo apt-get install libglut3-dev	# for glut.h

* SVN ignore

  http://wolfram.kriesing.de/blog/index.php/2005/svnignore-and-svnkeywords

  // -L/usr/X11R6/lib -lXi -lXmu -lm -lXext -lX11

